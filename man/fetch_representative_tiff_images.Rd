% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_query_workflow.R
\name{fetch_representative_tiff_images}
\alias{fetch_representative_tiff_images}
\title{Fetch Representative TIFF Crops For Selected Cells}
\usage{
fetch_representative_tiff_images(
  object,
  sampled_cells,
  fetch_resize_mult = 2,
  max_images_per_sample = 3,
  red_channel = 2,
  green_channel = 3,
  blue_channel = 1,
  annotate_color = "yellow",
  cell_data_store = NULL,
  background_cell_color = "lightblue",
  sample_col = "unique_id"
)
}
\arguments{
\item{object}{A \linkS4class{CellQueryInfo} object.}

\item{sampled_cells}{Either a data frame containing a \code{sample_id} column or
a named list of data frames split by sample id.}

\item{fetch_resize_mult}{Expansion multiplier for each cell bounding box
before TIFF extraction.}

\item{max_images_per_sample}{Maximum number of representative cells/TIFF crops
to return per sample.}

\item{annotate_color}{Annotation color passed to
\code{\link[TiffPlotR:rect_annotate]{TiffPlotR::rect_annotate()}}.}

\item{cell_data_store}{Optional \linkS4class{CellDataStore} object. If provided,
all cells in the full data frame will be annotated in the background.}

\item{background_cell_color}{Color to use for background cell annotations.
Defaults to "lightblue".}
}
\value{
A named list keyed by sample id where each element is a list of
\code{TiffPlotR} image objects.
}
\description{
Uses selected sample TIFF paths in a \linkS4class{CellQueryInfo} object and
extracts representative annotated cell crops for each sample.
}
\examples{
\dontrun{
q <- CellQuery()
cell_df <- load_query_cell_data(q)
reps <- select_representative_cells(cell_df)
imgs <- fetch_representative_tiff_images(q, reps)
plot(imgs[[1]][[1]])
}
}
