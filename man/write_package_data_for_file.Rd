% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_wrangling-LAPTOP-T6S5THI3.R,
%   R/data_wrangling.R
\name{write_package_data_for_file}
\alias{write_package_data_for_file}
\title{Write per-sample RNAscope cell object data}
\usage{
write_package_data_for_file(file)

write_package_data_for_file(file)
}
\arguments{
\item{file}{Path to a combined RNAscope ObjectData CSV file.}
}
\value{
Invisibly returns the path to the metadata file written.

Invisibly returns the path to the metadata file written.
}
\description{
Reads a combined RNAscope cell object CSV file and splits it into one CSV
per image sample, writing them to the package data directory alongside a
metadata summary file. Run once per input file; exits early if the metadata
output already exists.

Reads a combined RNAscope cell object CSV file and splits it into one CSV
per image sample, writing them to the package data directory alongside a
metadata summary file. Run once per input file; exits early if the metadata
output already exists.
}
\examples{
\dontrun{
write_package_data_for_file("/path/to/ObjectData_Clean.csv")
}
}
